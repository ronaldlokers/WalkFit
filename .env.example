# Strava upload (optional — feature stays hidden if unset). See CLAUDE.md "Strava upload"
# section for the full setup (register a Strava API app, deploy oauth-proxy/).
VITE_STRAVA_CLIENT_ID=
VITE_STRAVA_PROXY_URL=

# Withings weight sync (optional — feature stays hidden if unset). Same proxy worker as
# Strava (see oauth-proxy/README.md "Register the Withings API app").
VITE_WITHINGS_CLIENT_ID=
VITE_WITHINGS_PROXY_URL=
